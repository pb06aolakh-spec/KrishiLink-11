# Copy this file to `.env` and fill the values before running `npm run dev`
# Example Postgres connection
DATABASE_URL=postgres://username:password@localhost:5432/dbname

# Optional: port to serve the app (defaults to 5000 if not set)
PORT=5000

# REPL_ID is used by some dev-only Vite plugins (optional)
# REPL_ID=

# Note: this project currently reads server-side env vars via process.env.
# To make `npm run dev` work on Windows, install cross-env:
#   npm install -D cross-env
# and then copy `.env.example` to `.env` and update the values.
